{
  "name": "passvault",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build/ && webpack && cp manifest.json build/ && cd build && zip -r passvault.zip ./* && cp passvault.zip passvault.xpi"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "html-webpack-plugin": "^2.30.1",
    "jquery": "^3.3.1",
    "vue": "^2.5.13",
    "vue-style-loader": "^4.0.2",
    "webpack": "^3.11.0"
  },
  "devDependencies": {
    "css-loader": "^0.28.9",
    "vue-loader": "^14.1.1",
    "vue-template-compiler": "^2.5.13"
  }
}
